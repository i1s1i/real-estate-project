# سجل التغييرات (Changelog)

## [النسخة 1.1.0] - تحديث شامل للأمان والأداء وتجربة المستخدم

### 🔒 الأمان (Security)
- تحديث `config.py` وتطبيق إعدادات الجلسة الآمنة:
  - تفعيل `SESSION_COOKIE_HTTPONLY`, `SESSION_COOKIE_SECURE`, `SESSION_COOKIE_SAMESITE='Lax'`.
- إضافة `WTF_CSRF_ENABLED` و `WTF_CSRF_TIME_LIMIT`.
- إضافة مصادقة `@admin_required` لحماية المسارات الحساسة في `pages.py`.
- تحسين تنقية وتحقق المدخلات في استعلامات التصفية في `api.py`.

### ⚡ الأداء (Performance)
- إضافة فهارس قواعد البيانات (Database Indexes) على الأعمدة الأكثر استعلاماً (مثل `property_type`, `listing_type`, `price`, `infrastructure_score`) لتسريع عمليات البحث.
- استخدام `Flask-Caching` و `Flask-Compress` في `__init__.py` لتقليل استهلاك النطاق الترددي وتحسين سرعة الاستجابة.
- استبدال التوليد العشوائي للأسعار بمنطق ثابت (Deterministic Seeding) في `api.py` لضمان استقرار الرسوم البيانية وعدم تقلبها عند التحديث المستمر.

### 🛡️ جودة الكود (Code Quality)
- بناء نظام معالجة أخطاء مخصص (Error Handlers) مع قوالب `404.html` و `500.html`.
- إضافة إعدادات تسجيل (Structured Logging) عبر مكتبة `logging` لتتبع الأحداث الحرجة والأخطاء.
- إصلاح التحذيرات الخاصة بـ `datetime.utcnow()` عبر استخدام `datetime.now(timezone.utc)`.

### 🎨 تجربة المستخدم والتصميم (UX & Taste Guidelines)
- **مراجعة واجهة الويب**: تم تطبيق معايير Vercel للتصميم وتجربة المستخدم.
- **تحديث الخطوط**: استبدال خط Inter بخط Outfit اللاتيني ليتناسب مع الواجهات العصرية مع التخلص من القوالب الافتراضية.
- **الألوان**: استبدال قالب الألوان الفاتح (البيج والنحاسي) بنمط عالي التباين (Cold Luxury) يجمع بين درجات الرمادي الداكن والأزرق الكوبالتي (Cobalt) لإعطاء إحساس بالاحترافية والفخامة.
- **الوصولية (Accessibility)**: إضافة `aria-hidden="true"` للأيقونات التجميلية، وإلزام تعيين أبعاد `width/height` للصور لمنع إزاحة المحتوى (CLS).
- **تنظيم العناصر**: تحسين بطل الصفحة (Hero Section) بنقل قسم الإحصائيات لأسفل البطل لتطبيق قاعدة "أقصى 4 عناصر نصية" للمساعدة في تركيز انتباه المستخدم.
- إضافة زر "العودة للأعلى" مع حركات انسيابية باستخدام CSS.
